services:
  - type: web
    name: analise-enem
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/app.py --server.port=$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13